export enum GameStage {
    INTRO, //Up until soul is unlocked, fairly short
    PRE_TAILS, //Up until first tail is gained, also fairly short
    TAILS, //Up until first 'real' prestige + bead gain, longer
    //may be more stage flags here
    ASCENSION //Up until ascension
}